import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'temnorid.inwaw.world',
  appName: 'Temnorid',
  webDir: 'dist/app/browser'
};

export default config;
